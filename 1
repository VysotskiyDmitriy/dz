let matrix = [1, 2, 3, 'a', 'b', 'c', '4', '5', '6'];
let sum = 0;

for(let i = 0; i<matrix.length; i++){
  let a = parseInt(matrix[i]);
  if(a>=0 || a<=0){
    sum += a;
  } else { 
   sum += 0};
}

console.log(sum);
